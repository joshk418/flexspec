version: "2"

run:
  timeout: 5m

linters:
  default: standard
  exclusions:
    rules:
      - linters:
          - errcheck
        source: "^\t*defer "

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/joshk418/flexspec
